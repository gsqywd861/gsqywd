# 媒体水印去除工具 — 改进计划

> 版本：v1.0 | 日期：2026-05-11 | 状态：待评审

---

## 1. 竞品功能分析

| 竞品 | 核心能力 | 优势 | 不足 |
|------|---------|------|------|
| **Media.io** | Web 端 AI 去水印，支持所有主流视频格式 | 跨平台兼容（iOS/Android/Linux）、保持原始画质、AI 智能修复自然 | 需上传文件至服务器，隐私性弱；免费版有次数/水印限制 |
| **HitPaw** | AI 像素级填充（非模糊），可去除 Logo/文字叠加 | 修复效果自然、过渡流畅、支持批量处理、提供桌面客户端 | 付费软件、闭源、文件需上传处理 |
| **Dewatermark.ai** | CNN + GANs 联合推理，自动检测水印位置 | 自动识别、5 秒 Demo 体验、输出无模糊痕迹 | 免费额度低、仅支持图片、依赖云端算力 |
| **123Apps** | 手动框选 + 传统算法去除 | 免费、轻量、适合一次性快速修复 | 功能单一、效果依赖手动选区精度、无 AI 修复 |
| **WatermarkRemove.io** | 浏览器内自动 AI 检测 | 极简 UI、无需注册、即开即用 | 仅支持图片、选区调整能力弱、格式支持有限 |

### 竞品共性趋势

1. **AI 修复已成标配**：传统模糊/插值算法已无法满足用户预期，像素级重建（LaMa、GANs）是行业基线
2. **自动检测是差异化关键**：用户希望"上传即处理"，手动框选是次优路径
3. **隐私与本地化是新卖点**：竞品均为云端处理，纯客户端方案具备天然隐私优势
4. **批量处理是刚需**：社交媒体创作者、电商运营者需要一次性处理多个文件

---

## 2. 本项目当前状态评估

### 已有能力

| 维度 | 现状 |
|------|------|
| **架构** | 纯客户端处理（隐私优先），Vue 3 + Vite 8 + TypeScript |
| **图像处理** | OpenCV.js（Telea/NS 传统修复）+ ONNX Runtime Web + LaMa AI 模型 |
| **视频处理** | FFmpeg.wasm（当前 v0.12.x），支持 H.264/H.265 |
| **交互** | 多区域拖拽/缩放手动画选择器、处理后自动下载 |
| **用户系统** | Supabase Auth（邮箱注册/登录）、IndexedDB 本地存储 |
| **格式支持** | 图片：JPG/PNG；视频：MP4/MOV |
| **测试** | Vitest 单元测试 + Playwright E2E 测试 |

### 核心差距

| 差距项 | 影响 |
|--------|------|
| 无处理前后对比功能 | 用户无法直观判断效果，降低信任度 |
| 选区完全手动 | 操作门槛高，与竞品"自动检测"差距明显 |
| 格式覆盖不足 | 不支持 WebP/GIF/WebM/MKV 等常见格式 |
| 无批量处理 | 用户一次只能处理一个文件，效率低 |
| 无撤销/重做 | 误操作后需重新上传文件，体验差 |
| 进度信息粗糙 | 视频处理时仅显示百分比，无剩余时间/速度 |

---

## 3. 优先级改进方案

### P0 — 必须立即改进（核心体验）

#### 1. 前后对比预览（Before/After Slider）

**问题**：用户处理后无法直观对比效果，降低信任感和转化率。

**方案**：
- 实现一个水平滑动对比组件，左侧显示原图/原视频帧，右侧显示处理后结果
- 支持拖拽分割线实时对比
- 图片处理结果页默认展示对比视图
- 视频处理结果提供关键帧对比截图

**技术实现**：
- 组件：`BeforeAfterSlider.vue`，基于双 Canvas 层叠 + CSS `clip-path` 或 `overflow: hidden`
- 交互：鼠标拖拽/触摸滑动分割线位置
- 视频：抽取处理前后各 3 帧（首/中/尾）生成对比图集
- 依赖：纯前端实现，无需额外库

**验收标准**：
- 分割线拖拽流畅，无闪烁
- 移动端触摸响应延迟 < 50ms
- 对比组件在所有已支持格式下正常工作

---

#### 2. 选区自动吸附（Smart Snap-to-Edge）

**问题**：手动框选水印时精度不够，选区边缘常覆盖非水印内容或遗漏水印。

**方案**：
- 框选时实时分析选区边缘的像素差异，自动向水印边界微调
- 基于 OpenCV 的边缘检测（Canny）或颜色聚类，识别水印区域轮廓
- 提供"吸附强度"调节（强/中/弱/关闭）

**技术实现**：
- 在 `WatermarkSelector` 组件的 `mouseup` 事件中触发边界优化
- 使用 OpenCV.js 的 `cv.Canny()` 或 `cv.findContours()` 提取边缘
- 对选区四边分别向外/向内搜索最优边界
- 优化结果以动画方式平滑过渡到用户

**验收标准**：
- 对半透明文字水印，吸附准确率 > 80%
- 边缘检测耗时 < 200ms（1080p 图片）
- 用户可一键撤销自动吸附结果

---

#### 3. 多格式支持扩展

**问题**：当前仅支持 JPG/PNG/MP4/MOV，无法覆盖用户常见使用场景。

**方案**：

| 类别 | 新增格式 | 优先级 |
|------|---------|--------|
| 图片 | WebP（无损/有损） | 高 |
| 图片 | GIF（静态帧 + 动画帧分别处理） | 中 |
| 图片 | TIFF | 低 |
| 图片 | BMP | 低 |
| 视频 | WebM（VP8/VP9） | 高 |
| 视频 | AVI | 中 |
| 视频 | MKV | 中 |
| 视频 | FLV | 低 |

**技术实现**：
- 图片：利用浏览器原生 `canvas.toBlob()` 和 `createImageBitmap()` 支持 WebP/BMP/TIFF 读取
- GIF：引入 `gif.js` 或 `omggif` 库，逐帧提取 → 去水印 → 重新编码
- 视频：FFmpeg.wasm 原生支持 WebM/AVI/MKV，只需在文件选择器 `accept` 属性中添加对应 MIME type
- 在上传组件中添加格式提示文案

**验收标准**：
- WebP 图片上传、处理、导出全流程正常
- WebM 视频可正常去水印并导出
- 不支持的格式给出友好错误提示（非崩溃）

---

#### 4. 批量处理

**问题**：用户一次只能处理一个文件，社交媒体创作者/电商运营者效率极低。

**方案**：
- 图片批量：支持一次上传多张图片（上限 20 张），共享相同水印选区位置（按比例适配）
- 处理队列：后台依次处理，队列进度实时显示
- 结果打包：全部处理完成后打包为 ZIP 下载（使用 JSZip）

**技术实现**：
- 上传组件支持 `multiple` 属性
- 新增 `BatchProcessor` 服务，管理处理队列
- 每张图片复用同一组选区坐标，按图片尺寸等比缩放
- 使用 `JSZip` 库在客户端打包，触发批量下载
- 队列状态存储在 Pinia store 中，支持暂停/取消/重试

**验收标准**：
- 一次上传 20 张 1080p 图片可正常进入队列
- 队列处理中可暂停、取消、调整顺序
- ZIP 下载包含所有处理结果，文件名带时间戳后缀

---

### P1 — 高优先级（功能增强）

#### 5. AI 自动检测水印

**问题**：手动框选操作门槛高，与竞品"上传即处理"体验差距大。

**方案**：
- 集成轻量级水印检测模型（如 YOLOv8-seg 的 Web 优化版）
- 上传图片后自动推理，高亮显示检测到的水印区域
- 用户可确认、删除或调整自动检测结果
- 作为可选功能，不替代手动框选

**技术实现**：
- 训练/转换一个轻量级 ONNX 模型（输入 640x640，输出水印边界框 + 置信度）
- 模型文件 ~5-15MB，首次使用时懒加载
- 使用 ONNX Runtime Web 推理，支持 WebGPU 加速
- 检测结果以半透明覆盖层展示在图片上

**验收标准**：
- 对常见平台水印（抖音/快手/小红书）检测准确率 > 70%
- 推理耗时 < 3s（1080p 图片，WebGPU 模式）
- 用户可一键清除所有自动检测框

---

#### 6. 处理历史记录

**问题**：用户处理后关闭页面即丢失结果，重新处理需再次上传文件。

**方案**：
- 在 IndexedDB 中记录最近 N 条处理记录（默认 10 条）
- 每条记录包含：原始文件缩略图、处理参数、结果缩略图、时间戳
- 提供历史列表页，支持重新下载或重新处理
- 登录用户可选择同步至 Supabase

**技术实现**：
- 扩展 `local-storage.ts` 服务，新增 `history` 表
- 使用 IndexedDB 存储 Blob 数据（图片/视频缩略图）
- 新增 `HistoryPanel.vue` 侧边栏组件
- 存储配额管理：超出上限时自动清理最早记录

**验收标准**：
- 历史记录在页面刷新后仍存在
- 单条记录可重新下载原始结果文件
- 存储占用不超过 500MB（可配置）

---

#### 7. 撤销/重做

**问题**：误操作后需重新上传文件，操作成本过高。

**方案**：
- 维护一个操作栈（Command Pattern），记录每个关键操作
- 支持快捷键 Ctrl+Z（撤销）和 Ctrl+Shift+Z / Ctrl+Y（重做）
- 操作类型包括：添加选区、删除选区、调整选区大小、切换算法、调整参数

**技术实现**：
- 创建 `useUndoRedo` composable，管理操作栈
- 每个操作实现 `execute()` 和 `undo()` 方法
- 状态快照使用结构化克隆（`structuredClone`）或差量记录
- UI 显示撤销/重做按钮及可用步数

**验收标准**：
- 至少支持 20 步撤销/重做
- 撤销/重做响应时间 < 100ms
- 快捷键在桌面端正常工作

---

#### 8. 导出格式选择

**问题**：输出格式固定，用户无法根据需求选择。

**方案**：
- 图片处理结果允许选择输出格式（PNG/JPEG/WebP）
- 视频处理结果允许选择输出格式（MP4/WebM）和质量参数
- 提供格式说明（如"PNG 无损但文件大，JPEG 文件小但有损"）

**技术实现**：
- 处理完成页新增"导出设置"面板
- 图片：`canvas.toBlob(type, quality)` 控制输出
- 视频：FFmpeg.wasm 输出时指定编码参数
- 格式选择结果持久化到 localStorage（下次默认使用）

**验收标准**：
- PNG/JPEG/WebP 三种格式均可正常导出
- 视频 MP4/WebM 切换正常
- 文件大小与选择的质量参数匹配预期

---

#### 9. 进度实时显示（视频处理）

**问题**：视频处理时仅显示百分比，用户无法预估等待时间。

**方案**：
- 显示：当前帧/总帧数、已用时间、预估剩余时间、处理速度（帧/秒）
- 进度条支持暂停/继续操作
- 处理完成后显示总耗时和文件大小

**技术实现**：
- FFmpeg.wasm 的 `progress` 回调已提供当前帧信息
- 使用滑动窗口算法计算实时处理速度
- 剩余时间 = 剩余帧数 / 平均帧率
- 进度组件：`VideoProgress.vue`，支持展开/收起详细信息

**验收标准**：
- 剩余时间预估误差 < 20%（处理 30 秒后趋于稳定）
- 进度信息每 500ms 更新一次
- 暂停/继续操作不丢失已处理进度

---

#### 10. 移动端适配优化

**问题**：移动端触摸操作体验不够完善，选区调整不够精确。

**方案**：
- 双指缩放：支持在预览图上双指捏合缩放
- 双指旋转：支持双指旋转调整选区角度（适用于倾斜水印）
- 长按拖拽：长按选区进入拖拽模式
- 触摸反馈：选区操作时提供触觉反馈（振动 API）

**技术实现**：
- 使用 Pointer Events API 统一处理鼠标和触摸
- 实现 pinch-to-zoom 手势：监听 `pointerdown` × 2 → 计算距离变化 → 缩放
- 选区调整手柄增大至 44x44px（符合移动端最小触控面积标准）
- 使用 `navigator.vibrate()` 提供操作反馈（需用户授权）

**验收标准**：
- 双指缩放流畅，无延迟感
- 选区拖拽手柄在手机上易于点击
- 所有桌面端快捷键操作在移动端有等效手势

---

### P2 — 中优先级（体验提升）

#### 11. 模板预设

提供常见平台水印位置的一键选择模板：

| 平台 | 默认位置 | 选区预设 |
|------|---------|---------|
| 抖音 | 右下角 | 10% 宽高比例 |
| 快手 | 右下角 | 8% 宽高比例 |
| 小红书 | 左上角 | 12% 宽高比例 |
| Bilibili | 右下角 | 8% 宽高比例 |
| YouTube | 右下角 | 自定义 |
| 微博 | 右下角 | 10% 宽高比例 |

**技术实现**：
- 创建 `WatermarkTemplates.ts`，定义各平台预设参数
- UI 以卡片网格展示模板，点击即应用
- 用户可自定义模板并保存

---

#### 12. 视频抽帧预览

**方案**：
- 上传视频后，自动抽取关键帧（首/中/尾 + 手动选择）
- 用户在关键帧上框选水印区域
- 选区自动应用到整个视频时间线
- 支持在时间轴上滑动预览不同帧

**技术实现**：
- FFmpeg.wasm 抽帧：`ffmpeg -i input.mp4 -vf "select=eq(n\,0)+eq(n\,N/2)+eq(n\,N-1)" -vsync vframe 3 frames_%d.png`
- 使用 Canvas 渲染帧预览
- 时间轴组件支持拖动预览

---

#### 13. 动态水印支持（轨迹追踪）

**方案**：
- 对视频中移动的水印（如滚动字幕、游动 Logo）提供轨迹追踪
- 用户在第 1 帧标注水印位置，后续帧自动追踪
- 使用光流法（Optical Flow）或模板匹配追踪位置变化

**技术实现**：
- OpenCV.js 的 `cv.calcOpticalFlowPyrLK()` 实现 Lucas-Kanade 光流追踪
- 每 N 帧重新校准一次追踪结果
- UI 上以轨迹线展示水印移动路径

---

#### 14. 处理队列管理

**方案**：
- 多文件进入队列后，后台自动依次处理
- 队列面板显示：排队中/处理中/已完成/失败状态
- 支持拖拽调整处理顺序
- 失败项目可单独重试

---

#### 15. 快捷键支持

| 快捷键 | 操作 |
|--------|------|
| Ctrl+Z | 撤销 |
| Ctrl+Shift+Z / Ctrl+Y | 重做 |
| Ctrl+S | 下载处理结果 |
| Delete / Backspace | 删除当前选区 |
| Ctrl+A | 全选选区 |
| Esc | 取消当前操作 |
| Space | 播放/暂停视频预览 |

**技术实现**：
- 创建 `useKeyboardShortcuts` composable
- 全局事件监听，根据当前页面上下文分发
- 快捷键列表可通过 `?` 键唤出帮助面板

---

### P3 — 长期规划（差异化竞争力）

#### 16. WebGPU 加速

**目标**：利用 WebGPU 将 AI 处理速度提升 3-10 倍。

**方案**：
- 将 LaMa 模型的推理后端从 WebGL 切换至 WebGPU
- OpenCV.js 的计算密集型操作迁移至 WebGPU compute shader
- 浏览器兼容性降级：WebGPU 不可用时自动回退到 WebGL/CPU

**技术调研**：
- ONNX Runtime Web 已支持 WebGPU backend（`executionProviders: ['webgpu']`）
- 需测试 LaMa 模型在 WebGPU 下的兼容性
- OpenCV.js 暂无原生 WebGPU 支持，需自行实现 compute shader 或等待社区更新

---

#### 17. 反向 Alpha 混合算法

**目标**：针对 AI 生成图片（Gemini、DALL-E、Midjourney）水印的数学精确去除。

**原理**：
- AI 生成图片的水印通常以固定 Alpha 值叠加在原始像素上
- 公式：`result = foreground * alpha + background * (1 - alpha)`
- 已知 `result` 和 `alpha`（水印通常半透明），可反推 `background`

**方案**：
- 用户指定水印 Alpha 值（或通过自动检测估算）
- 对水印区域像素执行反向混合计算
- 适用于 AI 平台固定位置半透明水印

---

#### 18. Chrome 浏览器插件

**目标**：用户可在任意网页中右键选择图片，直接调用去水印功能。

**方案**：
- 开发 Chrome Extension（Manifest V3）
- 右键菜单添加"去除水印"选项
- 插件将图片数据传递给本地运行的 Web 应用（或通过 Content Script 内联处理）
- 支持当前标签页直接覆盖显示处理结果

---

#### 19. 社交分享

**方案**：
- 处理完成后提供一键分享按钮
- 支持平台：Twitter/X、Facebook、微博、微信二维码
- 分享内容：处理前后对比图 + 工具链接
- 可选：添加"由 Media Watermark Remover 处理"水印（默认关闭）

---

#### 20. 多语言支持（i18n）

**方案**：
- 使用 `vue-i18n` 实现国际化
- 首批支持语言：简体中文、English
- 后续扩展：日本語、한국어、Español、Français
- 语言切换在设置面板中完成，自动检测浏览器语言

---

## 4. 技术架构优化建议

### 4.1 FFmpeg.wasm 版本升级

**当前版本**：v0.12.x
**目标版本**：v0.13.x

**收益**：
- 更好的编码质量和压缩效率
- 更完善的编解码器支持
- 更稳定的 SharedArrayBuffer 兼容性

**迁移注意**：
- v0.13.x 的 API 有部分 breaking changes，需检查 `FFmpeg` 实例创建和 `exec()` 调用方式
- 更新 `@ffmpeg/util` 至匹配版本
- 测试现有视频处理流程的兼容性

---

### 4.2 OpenCV.js Web Worker 加载

**问题**：OpenCV.js WASM 文件约 10MB，在主线程加载会阻塞 UI 渲染。

**方案**：
- 在 Web Worker 中加载 OpenCV.js
- 主线程通过 `postMessage` 传递图像数据和处理指令
- Worker 返回处理结果（ImageBitmap 或 ArrayBuffer）

**技术实现**：
- 已有的 `workers/` 目录可扩展 `opencv-worker.ts`
- 使用 `Comlink` 简化主线程与 Worker 之间的 RPC 通信
- 预加载策略：首页空闲时静默加载 Worker，用户进入图片处理页时即可直接使用

---

### 4.3 WebCodecs API 替代方案

**目标**：对简单视频操作（解码/编码），使用浏览器原生 WebCodecs API 替代 FFmpeg.wasm。

**收益**：
- 原生 API 性能优于 WASM 方案
- 减少 WASM 模块加载体积
- 更细粒度的帧级控制

**适用场景**：
- 视频解码 → Canvas 渲染（预览用）
- Canvas → 视频编码（导出用）
- 不适用：格式转换、复杂滤镜（仍用 FFmpeg.wasm）

**浏览器支持**：Chrome 94+、Edge 94+、Firefox 127+

---

### 4.4 OffscreenCanvas 图片处理

**目标**：将图片渲染和像素操作从主线程转移至 OffscreenCanvas。

**方案**：
- `canvas.transferControlToOffscreen()` 将 Canvas 控制权转移至 Worker
- 在 Worker 中执行 OpenCV 处理、LaMa 推理结果渲染
- 主线程只负责 UI 交互和最终展示

**收益**：
- 主线程不再被图像处理阻塞，UI 保持流畅
- 处理大图片时不会出现"页面无响应"警告

---

## 5. 用户体验流程优化

### 3 步极简操作流程

```
┌─────────────────┐    ┌──────────────────┐    ┌──────────────────┐
│   Step 1: 上传   │───▶│  Step 2: 选择     │───▶│  Step 3: 处理     │
│                 │    │                  │    │                  │
│  · 拖拽文件      │    │  · AI 自动检测    │    │  · 一键开始       │
│  · 粘贴截图      │    │  · 手动框选       │    │  · 实时进度       │
│  · URL 输入      │    │  · 模板预设       │    │  · 自动下载       │
│                 │    │  · 多区域编辑      │    │  · 前后对比       │
└─────────────────┘    └──────────────────┘    └──────────────────┘
```

### 流程详细说明

#### Step 1 — 拖拽上传（或粘贴截图）

- 首页即上传区域，无多余导航
- 支持：拖拽文件、点击选择、Ctrl+V 粘贴剪贴板图片
- 自动识别文件类型，跳转至对应处理页面
- 大文件显示加载进度，避免"假死"感

#### Step 2 — 自动/手动选择水印区域

- 默认触发 AI 自动检测（如可用）
- 自动检测结果以高亮框展示，用户可：
  - 确认使用
  - 拖拽调整边界
  - 删除不需要的检测框
  - 手动添加新选区
- 提供模板预设快捷入口（侧边栏）

#### Step 3 — 一键处理并自动下载

- 点击"开始处理"按钮
- 图片：处理后在结果页展示 Before/After 对比，自动触发下载
- 视频：显示详细进度面板，完成后自动下载
- 处理结果同时保存至本地历史记录

---

## 6. 实施路线图（建议）

| 阶段 | 时间 | 包含项目 | 里程碑 |
|------|------|---------|--------|
| **Phase 1** | 第 1-2 周 | P0-1 前后对比、P0-3 多格式、P0-4 批量处理 | 核心体验闭环 |
| **Phase 2** | 第 3-4 周 | P0-2 选区吸附、P1-8 导出选择、P1-9 进度显示、P1-7 撤销重做 | 操作体验完善 |
| **Phase 3** | 第 5-7 周 | P1-5 AI 自动检测、P1-6 历史记录、P1-10 移动端优化 | AI 能力引入 |
| **Phase 4** | 第 8-10 周 | P2-11 模板预设、P2-12 视频抽帧、P2-15 快捷键、技术架构优化 | 体验深度打磨 |
| **Phase 5** | 第 11-14 周 | P2-13 动态水印、P2-14 队列管理、P3-20 i18n | 高级功能 |
| **Phase 6** | 第 15-20 周 | P3-16 WebGPU、P3-17 Alpha 混合、P3-18 浏览器插件 | 差异化壁垒 |

---

## 7. 风险与依赖

| 风险项 | 影响 | 缓解措施 |
|--------|------|---------|
| FFmpeg.wasm v0.13 兼容性 | 视频处理功能可能暂时不可用 | 先在独立分支验证，确认兼容后再合并 |
| AI 检测模型体积过大 | 首次加载慢，影响用户体验 | 模型懒加载 + CDN 分发 + 压缩至 < 10MB |
| WebGPU 浏览器覆盖率 | 部分用户无法享受加速 | 多级降级策略：WebGPU → WebGL → CPU |
| IndexedDB 存储配额 | 大容量视频可能超出浏览器配额 | 存储前检查可用空间，超出时提示清理旧记录 |
| OpenCV.js Worker 通信开销 | 小图片处理反而变慢 | 按图片尺寸动态选择主线程或 Worker 处理 |
